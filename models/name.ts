// database name
export const dbName = "stackOverflowClone";

// it maybe like name=ing the columns
export const questionCollections = "questions"
export const answerCollections = "answers"
export const commentCollections = "comments"
export const voteCollections = "votes"

// so question will be used as a basepoint and then everything else will be attached to it
export const questionAttachmentBucket = "question-attachment"